import React from 'react';
import RegisterForm from '../components/user/RegisterForm'; // Import the form

const RegisterPage: React.FC = () => {
    return (
        <div>
            {/* You can add titles or other elements specific to the page here */}
            <RegisterForm />
        </div>
    );
};

export default RegisterPage;